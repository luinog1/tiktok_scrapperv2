# URL reachable by the Next.js server, never by the browser directly.
BFF_URL=http://localhost:3000
BFF_API_KEY=
# Forward x-scrape-provider only in controlled admin/dev deployments.
ALLOW_PROVIDER_OVERRIDE=false
